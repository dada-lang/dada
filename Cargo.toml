[workspace]
members = [".", "components/*"]

# Exclude xtask so it isn't installed with `cargo install`
default-members = [
  ".",
  "components/dada-*",
]

[package]
name = "dada"
version = "0.1.0"
edition = "2021"
default-run = "dada"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

#[[test]]
#name = "harness"
#harness = false

[dependencies]
dada-lang = { path = "components/dada-lang" }
dada-3p = { path = "components/dada-3p" }
