#:spec syntax.string-literals.escape-sequences.close-brace
#:skip_codegen

# 💡 Using balanced \{ and \} in same string because the tokenizer's
# delimited() scanner doesn't skip string contents — unbalanced braces
# inside strings confuse brace-depth tracking. This is a known bug that
# will be fixed when implementing interpolation (which requires the same fix).
async fn main() {
    print("\{\}").await
    print("before\{middle\}after").await
}
