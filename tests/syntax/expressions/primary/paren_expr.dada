#:spec syntax.expressions.primaryexpr-definition.paren-expr
#:skip_codegen
#:FIXME

fn main() {
    # parenthesized expression changes precedence
    let x = (1 + 2) * 3
    set(x)

    # nested parentheses
    let y = ((42))
    set(y)
}

fn set(x: u32) {
}
