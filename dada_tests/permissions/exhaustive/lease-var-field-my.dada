class Pair(a, b)

async fn main() {
    let p = Pair(Pair(22, 44), 66)
    let q = p.a.lease
    print(q).await #! OUTPUT Pair\(22, 44\)
    print(p).await #! RUN ERROR cannot access leased data
    print(q).await # (note future used of `q` here)
}