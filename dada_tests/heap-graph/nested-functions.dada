#! OUTPUT ANY

class Point(any x, any y)

async fn main() {
    name = "Fellow Dadaist"
    helper().await
    print("Hello").await
    print(name).await
}

async fn helper() {
    p = Point(22, 44)
    q = Point(p, 66)
    #?             ^ HeapGraph 
    #
    # Test that we see the values from `main`.
}
