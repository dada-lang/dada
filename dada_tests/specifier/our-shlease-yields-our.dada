class Point(x, y)

async fn main() {
    p = Point(22, 33).share

    # Under current semantics, shleasing an `our`
    # yields another `our` value (not, e.g., shleased),
    # so this code works.
    x = p.x.shlease.share
}
