class Value(n)

# With parens
class List1(v: my{} Value) #! ERROR no paths are needed after the `my` permission
class List2(v: our{} Value) #! ERROR no paths are needed after the `our` permission
class List3(v: shared{} Value)
class List4(v: leased{} Value) #! ERROR leased permissions need at least one path
